---
interface Props {
  id?: string;
  className?: string;
}

const { id, className = '' } = Astro.props;
---

<div class={`max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 ${className}`} id={id}>
  <slot />
</div>
